<main>
    <form class="search" method="post">
        <input type="search" name="search" placeholder="Search here..." value="{{search}}">
        <button type="submit">Search</button>
    </form>
    <section id="find-section">

        {{#if search}}<!-- If there are any matching matches from the search, show each of them -->
        {{#if instances}}
        {{#each instances}}
            {{>'searchCard'}}
        {{/each}}

        {{else}}
            <div class="no-data-listing">
            <p class="no-offer">No match was found for the submitted type...</p>
        </div>
        {{/if}}
        {{/if}}

        <!-- If there are no search matches, show: -->
        <!---->
    </section>
</main>