<main>
    <section id="my-posts">
        <h1>My post.</h1>

        <div class="my-container">
            {{#if instances}}
                {{#each instances}}
                    {{>'profileCard'}}
                {{/each}}
            {{else}}
            <div class="no-posts">
                <div class="no-posts-img">
                    
                    <img src="/static/img/bear.jpg" alt="image_nature_3">
                </div>
                <p class="no-offer">There are no own posts yet...</p>
            </div>
            {{/if}}<!-- If there are own posts for wildlife in the database display each of them -->


            <!-- If there are still no own posts for wildlife in the database display: -->
            <!---->
        </div>
    </section>

</main>